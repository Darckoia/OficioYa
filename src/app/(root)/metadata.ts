import { buildMetadata } from '@/lib/seo';

export const rootMetadata = buildMetadata({
  title: 'OficioYa | Vende mejor por WhatsApp',
  description: 'Landing y demos interactivas para mostrar chat de ventas, vitrinas y bibliotecas de mensajes listas para negocio.',
  path: '/'
});
